import {
    StyleSheet
} from 'react-native'

export default StyleSheet.create({
    button: {
        margin: 5,
        fontWeight: 'bold',
        textAlign: 'center',
        borderWidth: 1,
        borderRadius: 5,
        color: '#fff',
        backgroundColor: '#33b17c',
        borderColor: '#33b17c',
        alignItems: 'center',
        padding: 10,
        flexDirection: 'column',
    }
})